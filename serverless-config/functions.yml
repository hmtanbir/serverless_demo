# test function
hello:
  handler: src/handler/handler.hello
  events:
    - http:
        path: /api/hello
        method: POST
        cors:
          origin: "*"

#routes for user API

getAllUser:
 handler: src/handler/user.getAllUsers
 events:
  - http:
      path: /api/v1/users
      method: GET
      cors:
       origin: "*"

getUser:
 handler: src/handler/user.getUser
 events:
  - http:
      path: /api/v1/users/{id}
      method: GET
      cors:
       origin: "*"

creatUser:
 handler: src/handler/user.createUser
 events:
  - http:
      path: /api/v1/users
      method: POST
      cors:
       origin: "*"

updateUser:
 handler: src/handler/user.updateUser
 events:
  - http:
      path: /api/v1/users/{id}
      method: PUT
      cors:
       origin: "*"

deleteUser:
 handler: src/handler/user.deleteUser
 events:
  - http:
      path: /api/v1/users/{id}
      method: DELETE
      cors:
       origin: "*"

# routes for product API
getAllProducts:
 handler: src/handler/product.getAllProducts
 events:
  - http:
      path: /api/v1/products
      method: GET
      cors:
       origin: "*"

getProduct:
 handler: src/handler/product.getProduct
 events:
  - http:
      path: /api/v1/products/{id}
      method: GET
      cors:
       origin: "*"

creatProduct:
 handler: src/handler/product.createProduct
 events:
  - http:
      path: /api/v1/products
      method: POST
      cors:
       origin: "*"

updateProduct:
 handler: src/handler/product.updateProduct
 events:
  - http:
      path: /api/v1/products/{id}
      method: PUT
      cors:
       origin: "*"

deleteProduct:
 handler: src/handler/product.deleteProduct
 events:
  - http:
      path: /api/v1/products/{id}
      method: DELETE
      cors:
       origin: "*"

# routes for order API
getAllOrders:
 handler: src/handler/order.getAllOrder
 events:
  - http:
      path: /api/v1/orders
      method: GET
      cors:
       origin: "*"

getOrder:
 handler: src/handler/order.getOrder
 events:
  - http:
      path: /api/v1/orders/{id}
      method: GET
      cors:
       origin: "*"

creatOrder:
 handler: src/handler/order.createOrder
 events:
  - http:
      path: /api/v1/orders
      method: POST
      cors:
       origin: "*"

updateOrder:
 handler: src/handler/order.updateOrder
 events:
  - http:
      path: /api/v1/orders/{id}
      method: PUT
      cors:
       origin: "*"

deleteOrder:
 handler: src/handler/order.deleteOrder
 events:
  - http:
      path: /api/v1/orders/{id}
      method: DELETE
      cors:
       origin: "*"
